# Tasks

- [x] `t1` R1 — Criar src/session/roles.rs com enum AgentRole e presets
- [x] `t2` R2 — ToolRegistry::for_role em src/tools/mod.rs
- [x] `t3` R3 — Papel na Session e TurnDeps/ToolContext
- [x] `t4` R4 — System prompt por papel em agent_loop.rs
- [x] `t5` R5 — Persistência do papel (migração 0005 + db.rs)
- [x] `t6` R6 — Papel no Project Context (store.rs + tools/context.rs)
- [x] `t7` R7 — Interface (sessions.rs + coder.rs)
